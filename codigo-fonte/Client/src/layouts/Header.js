import './Header.css'
import webimob_logo from '../images/logo.png'
const Header = ()=> {
    return (
        <header>
            <img src={webimob_logo} alt={'Logo Webimob'}/>
        </header>
    )
}

export default Header
